package player

func (p *Player) bhvLeadBall() string {
	cmd := ""
	// TODO: this behavior should assume p.GetBall() is accurate about ball position
	// it should make the agent run for it and lead it towards the positive X direction
	// making any neck/body orientation corrections along the way
	return cmd
}
